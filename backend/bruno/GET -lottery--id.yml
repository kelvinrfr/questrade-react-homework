info:
  name: GET /lottery/:id
  type: http
  seq: 2

http:
  method: GET
  url: "{{base}}/lottery/:id"
  params:
    - name: id
      value: "{{id}}"
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
