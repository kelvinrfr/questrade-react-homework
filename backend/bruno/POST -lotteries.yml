info:
  name: POST /lotteries
  type: http
  seq: 3

http:
  method: POST
  url: "{{base}}/lotteries"
  body:
    type: json
    data: |-
      {
        "type": "simple",
        "name": "Summer Giveaway",
        "prize": "MacBook Pro"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
